//mảng 2 chiều: datatype arrayname[x][y]; với x, y biết trc
//row : hàng ( chạy từ 0 -> x-1)
// collum: cột( chạy từ 0 -> y-1)
//các ô nhớ liên tiếp nhau
#include <stdio.h>
typedef For(i, a, b) for (i=0; i<a; i++)
int main(int argc, char const *argv[])
{
    int a[3][4];
    
    return 0;
}
